// This test should FAIL compilation — unsafe is not allowed in user code.
// Expected error: "unsafe blocks are not allowed in user code"

package main

fn main() -> i32 {
    unsafe {
        let x: i64 = 42;
    }
    return 0;
}
