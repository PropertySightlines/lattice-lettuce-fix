// ownership_test.salt

extern fn alloc_page() -> Owned<Window<u8, RAM>>;
extern fn free_page(page: Owned<Window<u8, RAM>>);

fn main() -> i32 {
    let p = alloc_page();
    free_page(p);
    
    // This should fail: p has been moved
    free_page(p);
    
    move 0;
}
